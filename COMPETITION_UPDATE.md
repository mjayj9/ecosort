# EcoSort 대회 업데이트 · 1단계

기준: 2026-09-07, Android 1.2(versionCode 3), V2 계약, 규칙 2026-09-07.1.

## 실제 구현과 변경 전후

| 항목 | 변경 전 | 현재 구현 |
|---|---|---|
| 사용자 상태 | 사진에서 개봉·사용 여부까지 추정 | 목적과 미개봉/사용 상태를 직접 선택, 기본 선택 없음 |
| AI 역할 | 오염 점수·세척·배출 문장을 한 번에 생성 | 물품·표시·관찰 가능 범위 등 제한된 코드값만 반환 |
| 미개봉 제품 | 현재 세척이 필요하다는 결과 가능 | 사용 후 미리 보기 또는 내용물 처리 준비로 구분 |
| 닫힌 뚜껑 | 미개봉 오인 시 진행 차단 가능 | 사용자 사용 상태를 우선하고 내용물·잔여물을 별도 확인 |
| 오염도·확률 | 0~100 점수와 자체 신뢰도 표시 | 화면에서 제거. 내부가 안 보임과 깨끗함을 구분 |
| 안내 | 자유 생성 문장·혼합 언어 | 검토한 한국어 명칭·준비 단계·공식 출처 |
| 상태 수정 | 다시 사진 분석 | 인증된 본인 기록을 서버에서 재판정, NIM 재호출 없음 |
| 지역 수거 | AI 결과가 배출 확정처럼 보임 | 모든 결과는 공통 준비 안내, 최종 수거 가능 여부 미확정 |

사진 촬영/선택, 축소·압축, 실제 Firebase 익명 Auth/App Check, 서버 NVIDIA 호출, 엄격한 JSON 검증, 오류·재시도, 상태 수정과 본인 계정 삭제가 연결되어 있다. JSON 누락·빈 응답·파싱 실패는 성공 결과로 바꾸지 않는다. 같은 요청 재시도는 중복 분석을 피하고, 새 사진은 이전 답변·결과를 초기화한다.

지원 범위는 음료팩, 음료용 PET병, PET/PP/PE 식품 용기, 음료·식품 금속캔, 온전한 음료·식품 유리병, 코팅되지 않은 종이상자의 공통 준비다. 위험 의심·지원 밖 품목·여러 물품·확인 불가 정보는 보류한다. 음료팩을 일반 종이로 안내하지 않는다.

**미구현/계획:** 독자 학습 모델, 실시간 영상 인식, 자동 OCR·바코드 재질 확인, 지자체 수거 DB, 검증된 오염 수치·정확도, 실제 배출 인증, 자동 포인트, 쿠폰 제휴, 단지 순위, 광고. 기존 보상 관련 시제품 코드 일부는 남아 있지만 앱에서 완료 서비스로 표시하지 않는다. 사진 판독은 실제 배출 인증이 아니다.

## Gemini → Kimi K3 → 현재 모델

기존 Gemini 3 Flash 표기를 정리했다. Kimi K3는 NVIDIA NIM 연동을 시도했지만 당시 반복 시간 초과로 성공하지 못했다. 사용자 승인으로 **Nemotron 3 Nano Omni**를 사용한다. 앱은 실제 모델명을 표시하며 K3 성공이나 자체 모델 개발로 소개하지 않는다. 모델 교체만으로 사진에 보이지 않는 내부·사용 이력을 알 수는 없어, 1단계에서는 관찰과 사용자 확인을 분리했다.

## 네 심사역량과 증거

| 역량 | 구현 증거 | 팀이 직접 남겨야 할 활동 |
|---|---|---|
| 가치창출 | 미개봉·내용물 남음·잔여물·재질별 분기, 종이팩 오안내 방지 | 실제 생활 문제와 사용 전후 인터뷰·실물 검증 |
| 도전역량 | Mock 제거, 실제 NIM/Firebase, 시간 초과·개봉 오인 실패와 수정 기록 | 실패 원인과 선택한 해결책 설명 |
| 자기주도 | 기준 버전과 ①~⑧ 커밋, 자동 테스트, 실제 검증 보고서 | 각자의 실행·검토·수정 내용을 날짜와 함께 작성 |
| 집단창의 | PDF 역할표와 검토 가능한 코드·시연·문서 | 역할별 실제 산출물과 상호 피드백 연결 |

제공 PDF 27쪽의 예선 배점은 가치30/도전30/자기주도20/집단창의20, 결선은 30/25/20/25로 확인한 기존 기록을 반영했다. PDF 18쪽 역할은 정민재(총괄·메인 코딩·실행), 채희범(코딩·검사·해결), 정서준(보조·오류·사용성), 김관호(계획·보고·검토)이다. **배정된 역할이며 실제 활동을 증명하는 것은 아니다.** 13쪽과 18쪽의 역할 차이는 팀이 확인해야 한다. AI 도구의 구현 지원도 공개하며 없는 활동·성과·제휴를 만들지 않는다.

## 3분 시연 순서

1. **0:00~0:25** — 미개봉 두유를 세척 대상으로 오인한 이전 화면과 문제를 설명한다. 새 앱은 목적·사용 상태를 확인한다고 소개한다.
2. **0:25~1:40** — 물품 한 개를 촬영하거나 실물 사진을 선택하고 실제 분석한다. 네트워크에 따라 약 40~70초 이상 걸릴 수 있어 기다리는 동안 키 보호·관찰/규칙 분리를 설명한다. 한 번에 끝난다고 약속하지 않는다.
3. **1:40~2:15** — 내용물/잔여물 질문에 실제 상태대로 답한다. ‘모르겠음’의 보류와 답변 수정의 빠른 반영을 보여 준다.
4. **2:15~2:40** — 미개봉 제품은 현재 오염 판정 대신 사용 후 미리 보기임을 확인한다. 다른 물품 상태를 일부러 입력하는 시연은 ‘상태 분기 테스트’라고 명시한다.
5. **2:40~3:00** — 공식 안내 출처·지역 확인 문구, 커밋/성공·실패 기록, 남은 실물 평가를 보여 준다.

NIM 지연·실패 시 오류와 재시도 자체를 보여 준다. 사전 영상은 사전 실행 자료라고 표시한다. 저장한 결과나 테스트 응답을 실시간 판독처럼 꾸미지 않는다. 원본 미개봉 두유 사진은 이번에 없어 실제 실물 재검증이 필요하다.

## 예상 질문 5개

1. **정확도는 몇 %인가요?** 정답이 있는 평가 데이터셋으로 측정한 수치는 없다. 자동 테스트와 실제 연결 검증을 구분해 공개한다. 같은 사진에서도 AI 결과가 달라질 수 있다.
2. **자체 AI인가요?** 현재는 NVIDIA의 Nemotron을 이용하고, 상태 확인·검증·한국어 안내 규칙을 직접 구현한 구조다. K3 성공과 독자 모델 학습은 아직 주장하지 않는다.
3. **미개봉을 왜 사용자에게 묻나요?** 사진만으로 사용 이력이나 가려진 내부를 확정할 수 없다. 미개봉 확인을 AI 추정으로 덮어쓰지 않고, 모르는 상태도 그대로 보류한다.
4. **키·사진은 어떻게 처리하나요?** NVIDIA 키는 Firebase Secret/서버 환경변수만 사용한다. 사진은 분석을 위해 Firebase와 NVIDIA로 전송되며 DB에는 원본 사진·원문 응답을 저장하지 않는다. 상태 재판정용 관찰·답변·결과는 서버 기록에 저장한다. 공급자의 처리까지 앱이 통제한다고 주장하지 않는다.
5. **배출 인증·포인트·지역 수거도 되나요?** 현재는 공통 준비 안내다. 최종 수거 가능 여부, 실제 배출 인증과 보상·제휴는 향후 계획이다.

## 사업계획서에서 수정할 문장

| 기존 주장/표기 | 사실에 맞춘 문장 |
|---|---|
| Gemini/Kimi K3 정상 서비스 | 현재 시연 모델은 NVIDIA NIM Nemotron 3 Nano Omni이며 K3 성공은 미확인 |
| EcoPick/에코픽과 EcoSort 혼재 | EcoSort · 에코소트로 통일 |
| 오염도 0~100과 정확도 보장 | 관찰 가능 범위와 사용자 확인을 구분하며 검증된 오염 수치·정확도는 없음 |
| 사진만으로 씻을지 버릴지 확정 | 물품 관찰 후 사용 상태·내용물·잔여물·표시를 확인하여 공통 배출 준비를 안내 |
| 전국 배출 기준 자동 반영 | 지역 수거 DB는 미구현이며 거주지 기준 확인 필요 |
| 독자 모델·실시간 자동 인식 완료 | 현재는 서버 멀티모달 모델을 이용한 사진 분석, 자체 모델·영상 인식은 향후 계획 |
| 자동 포인트·쿠폰·순위·광고 완료 | 실제 배출 인증과 확장 기능은 향후 계획 |
| Play Store 출시·보안 검증 완료 | 로컬 APK와 Firebase 배포를 확인했으며 Play Console·Release Play Integrity는 별도 검증 필요 |
| 제휴·매출·환경 효과 확보 | 실제 계약·매출·측정 자료가 있을 때만 기재 |

## 실행 방법

프로젝트는 `C:\Users\admin\StudioProjects\ecosort_1`, 작업 브랜치는 `phase1/state-aware-guidance`다. 현재 클라우드 서버는 V2이므로 새 APK 1.2를 설치한다. 기존 APK의 옛 결과 계약은 거부된다.

```powershell
cd C:\Users\admin\StudioProjects\ecosort_1
$env:JAVA_HOME = 'C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:assembleDebug
& "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 install -r .\app\build\outputs\apk\debug\app-debug.apk
& "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 shell am start -n com.aistudio.ecosort.kxmpzq/com.example.MainActivity
```

앱에서 ‘사진 분석 시작하기’로 실제 Firebase 익명 로그인 → 사진 선택/촬영 → 목적·사용 상태 선택 → 전송 동의 → 분석. 추가 질문에 답한 뒤 ‘확인한 상태로 안내 갱신’을 누른다. 기존 결과는 답변 수정 시 숨겨진다. 인터넷은 필요하며 클라우드 APK는 로컬 Firebase 서버 없이 실행된다.

새 Debug 기기는 앱 실행 후 Firebase CLI에 로그인된 PC에서 등록한다. 토큰을 화면·문서·Git에 복사하지 않는다.

```powershell
cd C:\Users\admin\StudioProjects\ecosort_1\functions
npm.cmd run register:device -- 기기ID
```

기기 ID는 `adb devices`로 확인한다. 현재 `emulator-5554`는 등록되어 있다. Debug는 시연/개발용이며 Release Play Integrity와 Google 계정 로그인 성공은 별도 확인이 남아 있다.

## NVIDIA_API_KEY 및 재현

현재 Firebase Secret `NVIDIA_API_KEY`와 전용 `ecosort-runtime` 실행 계정을 사용한다. 키 교체는 루트 `set-cloud-key.ps1`의 숨김 입력을 이용한다. 값은 코드·APK·.env·명령행 인자·로그·문서에 넣지 않는다.

```powershell
.\set-cloud-key.ps1
firebase.cmd deploy --only functions:analyzeImage --project focused-rig-vcf5x
```

클라우드의 비밀이 아닌 `NVIDIA_MODEL` 설정은 `functions/.env.focused-rig-vcf5x`에서 관리한다. 모델 ID는 `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning`이다. 허용 목록에 K3도 있지만 클라이언트가 키·모델·결과를 지정할 수 없다.

로컬은 루트 `start-local.ps1`의 숨김 입력이 키를 서버 프로세스 환경변수에만 넣는다. Android는 `-PecoLocal=true`로 별도 `.local` 패키지를 빌드한다. Functions/DB는 us-central1, 배포 런타임은 Node 22다. 기존 이름 있는 DB 5개는 변경하지 않았다.

```powershell
# functions 폴더: 자동 테스트
npm.cmd test
# Auth/Firestore/Functions 로컬 에뮬레이터가 실행된 상태
npm.cmd run test:state-emulator
npm.cmd run smoke:local -- 사진.jpg
# 실제 공개 병 사진으로 상태 분기 검증: 사용자 답변은 테스트 입력
npm.cmd run test:state-live -- cloud ..\docs\evidence\phase1\commons-empty-bottle.jpg ..\phase1-report.json
```

마지막 명령은 등록된 Debug 기기와 인터넷이 필요하며 실제 NIM 사용량이 발생한다. 기기 기본값은 emulator-5554다. 테스트 사진 출처와 실제 검증 성공·실패는 [검증 기록](VERIFICATION_REPORT.md), 복구는 [번호별 커밋](docs/PHASE1_CHECKPOINTS.md)을 참고한다.
