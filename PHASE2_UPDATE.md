# EcoSort 2단계 · 카메라 없이 검증하는 포장 인식

2026-09-07 · Android 1.3(versionCode 4) · 기존 Firebase V2 서버 사용

사용자가 제공한 매일두유 고단백 검은콩 190mL 사진 6장으로 **등록된 포장 디자인의 면과 위치를 실제 픽셀에서 찾는 기능**을 구현했다. 카메라 권한이나 서버 연결 없이 실행할 수 있다. 화면의 점과 사각형은 인식기의 계산 결과다.

## 이번에 완성한 범위

| 기능 | 동작 |
|---|---|
| 6면 움직임 실험 | 앞·뒤·성분·영양표시·위·아래 면 전환, 이동·회전·기울임·크기 조절 |
| 특징점 인식 | OpenCV ORB 매칭과 RANSAC으로 면·사각형·검증점을 계산 |
| 연속 프레임 확인 | 3회 연속 일치 후 안정 표시, 대상이 사라지면 즉시 표시 제거 |
| 반례 조작 | 일부 가림과 대상 숨김으로 인식 실패·재획득 확인 |
| 실제 사진 입력 | Android Photo Picker로 고른 사진에 같은 인식기 적용 |
| 기존 안내 연결 | 스캐너에서 진입한 경우, 인식된 실제 사진을 기존 상태 확인 화면에 전달 |
| 보안 | 과거 프로젝트 키만 폐기, 현재 키 유지, 실제 Firebase 설정 Git 제외, 커밋·CI 검사 |

사진을 고른 뒤 인식했다고 해서 개봉 여부나 내부 오염을 확정하지 않는다. 기존 분석 화면에서 목적·사용 상태를 직접 선택하고 전송에 동의해야 Firebase/NVIDIA 분석을 시작한다. 합성 프레임은 이 서버 연결 버튼을 제공하지 않는다.

## 왜 완전한 3D 대신 6면을 사용했는가

현재 검증하려는 것은 포장에 일부 무늬가 보일 때 그 면과 위치를 찾는 능력이다. 이 목적에는 각 면의 평면 변환으로 회전·기울임·크기·가림을 조절하는 방식이 직접적이다. 사진 6장만으로 새로운 각도·광택·찌그러짐을 충실하게 재현하는 3D 복원은 이번 범위에서 제외했다.

새로운 범용 AI 모델을 학습한 것은 아니다. 다른 두유 디자인, 찌그러진 팩, 실제 카메라 영상과 다양한 배경에서의 성능은 별도 자료로 확인해야 한다. 손 관절을 이해하는 의미 기반 랜드마크 모델과도 다르다.

## 인식 알고리즘

1. 원본 사진의 네 모서리를 지정해 면을 보정한다. 원본 해시와 보정 좌표는 `app/src/main/assets/carton/manifest.json`에 남겼다. 파생 JPEG에는 원본 EXIF를 넣지 않는다.
2. 입력 프레임을 회색조로 바꾸고 ORB 특징점·기술자를 계산한다. 선택 사진은 인식 전에 긴 변을 최대 640px로 줄인다.
3. Hamming 거리의 최근접 2개를 비교한다. 비율이 0.74 미만인 후보를 채택하고 같은 입력 점의 중복 매칭을 제거한다.
4. 최소 18개 매칭으로 RANSAC 호모그래피를 계산한다. 재투영 오차 기준 3.5px, 최대 2,000회, 신뢰 설정 0.995를 사용한다.
5. 최소 14개 기하 일치점, 일치 비율 0.55 이상, 참조면 가로·세로 각각 25% 이상 분포, 3×3 격자 중 4칸 이상을 요구한다.
6. 투영된 면은 유한한 볼록 사각형이어야 한다. 화면 면적의 1.2~95%, 각 변 22px 초과, 화면 바깥 허용폭 20% 기준으로 비정상 변환을 거른다.
7. 움직이는 입력은 같은 면이 3프레임 연속 일치할 때 안정 표시한다. 시간 간격 1,500ms 초과, 큰 위치 점프, 면 변경, 유실에는 누적 상태를 초기화한다. 정지 사진 한 장은 기하 검증 결과만 표시한다.
8. 직전 프레임에서 실제로 찾은 면을 다음 프레임에서 먼저 검사한다. 다시 검증해 35개 이상 일치점이 있을 때만 빠르게 반환하고, 부족하면 다른 면들도 검사한다.

렌더러는 움직인 **입력 이미지**만 만든다. 선택 면 ID, 정답 모서리, 이동 값을 인식기에 전달하지 않는다. 표시되는 점은 미리 그린 정답이 아니다. 서버의 오염·배출 결과를 이 인식 결과로 대체하지도 않는다.

## 바로 실행하기

제공된 Debug APK를 설치하고 앱 첫 화면에서 **카메라 없이 6면 인식**을 누른다. 이 기능은 로그인·카메라·NVIDIA 키 없이 동작한다.

1. `자동 이동`을 켜고 점과 사각형이 포장을 따라가는지 본다.
2. `다음 면`으로 면을 바꾼다. 슬라이더로 회전·기울임·크기를 바꾼다.
3. `일부 가림`, `대상 숨김`을 켜고 표시가 유지되거나 사라지는 조건을 본다.
4. `내 사진으로 인식`에서 제공한 원본 또는 새 사진을 고른다. 새 사진의 성공은 보장하지 않는다.
5. 서버 안내까지 보려면 로그인한 스캐너에서 `카메라 없이 6면 인식`에 들어간다. 실제 사진 인식 후 `이 사진으로 상태 확인` → 목적·사용 상태 선택 → 전송 동의 → 분석을 진행한다.

미개봉 두유 예시는 `사용 후 배출법 미리 보기`와 `미개봉·미사용`을 직접 선택한다. 재질 질문은 실제 표시를 보고 답한다. 이번 제공 사진의 표시 면에는 `멸균팩`이 보인다. 현재 세척이 필요하다거나 내부가 더럽다는 결과로 설명하지 않는다.

새 Debug 기기로 서버 분석을 하려면 기존 절차대로 App Check 디버그 등록이 필요하다. 로컬 면 인식에는 필요 없다. 이번 검증 기기 `emulator-5556`은 등록했다. NVIDIA는 실제 외부 서비스이므로 지연과 실패가 발생할 수 있다.

### 소스에서 빌드

```powershell
cd C:\Users\admin\StudioProjects\ecosort_1
.\configure-firebase.ps1 -Source '현재 프로젝트에서 받은 google-services.json 경로'
$env:JAVA_HOME = 'C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:assembleDebug :app:testDebugUnitTest
cd functions
npm.cmd test
```

이미 로컬 설정이 있는 작업 폴더에서는 설정 복사 단계를 반복할 필요가 없다. NVIDIA 키는 이번에 바꾸지 않았으며 기존 Firebase Secret `NVIDIA_API_KEY`를 사용한다. 앱에 키를 입력하지 않는다. 서버 로컬 실행과 Secret 설정은 기존 `COMPETITION_UPDATE.md`의 절차를 따른다.

## 검증 결과와 한계

- Debug·Release 빌드 성공. Android 단위 검사 25개, 서버 검사 98개 통과.
- Android 네이티브 인식 검사: 6면×3각도 합성 입력 18/18, 등록 원본 6/6 면 일치. 빈 화면과 다른 병 사진은 인식하지 않았다.
- 측정한 25개 처리 시간은 중앙값 226ms, 범위 54~446ms다. 같은 면을 먼저 검사하는 최적화 후 결과이며 에뮬레이터 측정이다.
- 실제 화면에서 자동 이동, 일부 가림, 대상 숨김, 실제 사진 선택·앞면 인식·스캐너 전달을 확인했다. 카메라 권한은 허용하지 않았다.
- 실제 Firebase/NIM 호출은 첫 시도에서 503 오류가 났다. 한 차례 수동 재시도에서 앞면 약 16.0초, 표시 면 약 61.8초에 성공했다. 재질 확인 후 두 경우 모두 사용 후 미리 보기로 이어졌다.

위 18개 합성 입력과 원본 6장은 등록 자료에서 나온 것이므로 **실물 정확도나 독립 시험 정확도로 발표하면 안 된다.** 30fps 실시간 카메라 제품으로도 표현하지 않는다. UI 갱신에는 인식 시간 외에 대기·렌더링 시간이 추가된다.

## 발표에 반영할 내용

| 심사역량 | 보여줄 사실 |
|---|---|
| 가치창출 | 제품을 발견하는 단계와 실제 사용·내용물·배출 준비 판단을 분리했다. 미개봉을 오염으로 단정하지 않는다. |
| 도전역량 | 실제 네이티브 매칭, 가림·유실 반례, 외부 AI의 503 실패와 재시도 기록을 제시한다. |
| 자기주도 | 번호별 커밋, 성능 개선 전후 측정, 빌드·회귀 검사, 키 노출 조치를 연결한다. |
| 집단창의 | 제공된 제품 사진과 실제 담당자의 활동 자료를 연결한다. 촬영자나 개발 역할·제휴를 추정해서 기재하지 않는다. |

3분 시연: 0:00~0:30 문제와 미개봉 오판 설명 → 0:30~1:15 6면 이동·특징점 → 1:15~1:40 대상 숨김·재획득 → 1:40~2:10 실제 사진 선택·상태 확인 → 2:10~3:00 실제 서버 결과와 검증 한계 설명. 서버가 지연되면 성공 기록과 실패 기록을 보여주되 실시간 응답처럼 설명하지 않는다.

사업계획의 ‘독자 AI 학습 완료’, ‘모든 쓰레기 실시간 인식’, ‘카메라로 오염도 정밀 측정’은 현 구현과 맞지 않는다. 다음처럼 수정한다: **“등록된 포장 디자인 한 종류의 6면 특징점 인식을 구현했고, 실제 사진의 배출 준비 안내는 Firebase/NVIDIA 분석과 사용자의 상태 확인을 결합한다.”**

예상 질문 5개:

1. **직접 학습한 모델인가?** 아니다. 이번 단계는 ORB와 기하 검증으로 등록 영상에 대응되는 부분을 찾는다.
2. **움직이는 화면의 답을 미리 아는가?** 입력은 합성이고, 인식기에는 영상 픽셀만 준다. 합성이라는 표기를 유지한다.
3. **이것만으로 오염을 아는가?** 알 수 없다. 상태 확인과 기존 서버 안내를 별도로 거친다.
4. **사진 6장 성공이면 실물도 잘 되는가?** 아직 증명하지 못했다. 다른 배경·조명·거리·새 촬영 자료로 독립 평가가 필요하다.
5. **오프라인에서도 되는가?** 로컬 면 인식은 된다. Firebase/NVIDIA 안내는 인터넷과 인증이 필요하다.

포인트·쿠폰·단지 순위·광고·범용 학습 모델·실시간 카메라 스트림·완전한 3D 복원은 구현하지 않았다. 이번 단계는 Play Store나 Firebase Functions를 새로 배포하지 않았다.

## 키와 복구 기록

현재 `focused-rig-vcf5x` 키와 프로젝트는 유지했다. 과거 `studio-4335756557-ed258` 키만 삭제했다. GitHub의 그 과거 키 경고 두 건은 폐기 완료로 처리했고 현재 키 경고는 남겼다.

현재 브랜치 파일에서 키 문자열은 제거했지만 **과거 Git 커밋을 지운 것은 아니다.** 복구 해시를 보존해 강제 푸시·기록 재작성을 하지 않았다. 현재 Firebase 클라이언트 식별자는 APK에 필요하며 비밀인 NVIDIA 키와 구별된다. 키를 숨기는 것만으로 Firebase 접근을 보호한다고 설명하지 않는다.

자세한 결과: [PHASE2_VERIFICATION.md](PHASE2_VERIFICATION.md), 복구: [docs/PHASE2_CHECKPOINTS.md](docs/PHASE2_CHECKPOINTS.md).

기술 근거: [OpenCV 특징점과 호모그래피](https://docs.opencv.org/4.x/d1/de0/tutorial_py_feature_homography.html), [OpenCV Android 설치](https://docs.opencv.org/4.x/d5/df8/tutorial_dev_with_OCV_on_Android.html), [Firebase API key 설명](https://firebase.google.com/docs/projects/api-keys). 사용자 참고 앱 [Make Me Cube Master](https://wooramsol.github.io/makemecubemaster/)의 코드나 모델은 복제하지 않았다.
